/**
 * Proxy configuration object
 * @typedef {Object} ProxyConfig
 * @property {string|string[]} url - The proxy URL (single or multiple)
 * @property {string} [key] - X-API-Key header value (optional)
 * @property {number} [rotateInterval=5000] - Proxy rotation interval in milliseconds (default: 5000)
 */

export const ProxyConfig = {}; // ✅ Use ES Module export